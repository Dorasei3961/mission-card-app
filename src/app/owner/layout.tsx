import type { ReactNode } from "react";

/** App Router セグメント `/owner` 用レイアウト（ページ本体は client component） */
export default function OwnerLayout({ children }: { children: ReactNode }) {
  return children;
}
